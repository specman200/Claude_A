__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Model weights. The station's own fine-tuned weights are committed (see
# models/); anything else — downloaded baselines, exports, experiments — is not.
*.pt
*.onnx
*.engine
!models/ppe-yolo11s.pt

# Runtime output
logs/
*.prof
